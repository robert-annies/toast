[defaults]
ask_vault_pass = False # if False you need to store the password in clear at below file

[privilege_escalation]
become_ask_pass = False # if False make sure target host allows password-less sudo for deploy user
